name: Production Build Android

on:
  push:
    branches: ["build-android-production"]

jobs:
  build_android:
    name: Build Android (Production)
    type: build
    params:
      platform: android
      profile: production
